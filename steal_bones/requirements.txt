# Steal Bones — зависимости.
#
# Это Flask/requests/sqlite3-стек, а не FastAPI/httpx/SQLAlchemy из исходного
# черновика ТЗ — см. README.md, раздел "Отличия от исходного ТЗ", про
# причину (в среде разработки не было сетевого доступа для pip install, а
# эти пакеты там уже стояли предустановленными и есть возможность их
# протестировать). Функционально ничего не потеряно: тот же интерфейс
# адаптеров, та же схема БД, та же логика rate-limit guard.

flask>=3.0
requests>=2.31
pandas>=2.0
openpyxl>=3.1
